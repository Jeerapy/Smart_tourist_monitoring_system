# Web smoketest

Minimal static page for verifying backend endpoints.

## Run
- Open `index.html` in a browser (or use any simple static server).
- Set backend base URL (default `http://localhost:8000`).
- Paste your Supabase `access_token`.

